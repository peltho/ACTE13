<div class="row">
    <p id="Description" class="col-lg-10 col-md-10 col-lg-offset-1 col-md-offset-1">
        Mauris eget est malesuada, aliquet leo at, malesuada diam. In placerat bibendum urna, sit amet dictum felis vestibulum eu
    </p>
</div>
<div class="row">
    <div class="col-lg-8 col-md-8">
        <ul id="ListNews" class="row">
            {% for article in articles %}
            <li class="col-lg-6 col-md-6 newsTpl">
                <article>
                    <img src="{{ article.thumbnail }}"/>
                    <div class="content">
                        <h3>{{ article.post_title }}</h3>
                        <p>{{ article.post_content }}</p>
                        <a href="{{ article.permalink }}">En savoir plus</a>
                        <p class="posted"><small>postée le {{ article.post_date|date('d/m/Y') }}</small></p>
                    </div>
                </article>
            </li>
            {% endfor %}
        </ul>
        <a id="MoreNews" href="{{ actualites }}" class="col-lg-12 col-md-12"><div>Voir toutes les actualités</div></a>
    </div>
    <div class="col-lg-4 col-md-4">
        <section id="Calendar">
            {{ calendrier|raw }}
        </section>
    </div>
</div>